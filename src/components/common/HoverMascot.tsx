import React from "react";

interface HoverMascotProps {
  children: React.ReactNode;
}

const HoverMascot = ({ children }: HoverMascotProps) => {
  return <>{children}</>;
};

export default HoverMascot;
